services:
  grading:
    build:
      context: .
      dockerfile: grading.Dockerfile
    volumes:
      - pyre-data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: "3"
          memory: 4G
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=64M,noexec,nosuid,nodev
    networks:
      - internal

  web:
    build:
      context: .
      dockerfile: web.Dockerfile
    ports:
      - "3000:3000"
    environment:
      - GRADING_SERVICE_URL=http://grading:8000
    depends_on:
      grading:
        condition: service_healthy
    networks:
      - internal
      - default

volumes:
  pyre-data:

networks:
  internal:
    internal: true
